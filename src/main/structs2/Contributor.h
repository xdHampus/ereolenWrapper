#pragma once

#include <string>
namespace ereol {
    struct Contributor {
        std::string type;
        std::string composedName;
        std::string firstName;
        std::string lastName;
    };
}
